# 003 — CI pipeline

**Status:** Done · **Band:** Foundation

**What.** GitHub Actions gate on every push/PR: `npm ci` → `npm run typecheck` → `npm test`.

**Files.** `.github/workflows/ci.yml`
