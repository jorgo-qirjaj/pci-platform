# 001 — Monorepo scaffold

**Status:** Done · **Band:** Foundation

**What.** npm-workspaces monorepo: `server` (Express+TS) and `web` (React+Vite), shared scripts
(`dev`, `build`, `typecheck`, `test`) at the root.

**Files.** `package.json`, `server/`, `web/`
