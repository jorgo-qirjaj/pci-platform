# 301 — Cases API

**Status:** Done · **Band:** Backend/Data

**What.** CRUD-ish case endpoints: list (lab-scoped, filterable), get, create, score, finalize,
report, annotation add/delete — all behind `requireAuth` + lab ownership.

**Files.** `server/src/routes/cases.ts`
